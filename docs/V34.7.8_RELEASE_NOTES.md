# V34.7.8 — BlaBla four-surface propagation

## Fixed

BlaBla-synchronized growth data now remains authoritative across My Roster, Precision, Simulation, and five-deck auto composition.

The five-deck optimizer previously rebuilt the shared growth context with a reduced field set. This could drop `favoriteItemPhase`, observed equipment attack/HP/defense, equipment slot count, and overload totals before calculation. The optimizer and stored-build projection now retain those fields, while survival calculation consumes observed equipment HP/defense.

## Verified

A real browser regression imports a BlaBla-shaped roster and checks the same Sugar Favorite record through all four surfaces. It verifies level, skill 2/7/9, Phase 3, Diffusion cube, observed equipment values, overload values, simulation output, and five-team candidate/selection calculations.

A live Cloudflare preflight also verifies the production origin and same-origin `/api/blabla/sync` bridge with a public profile. The post-merge workflow waits for the automatic V34.7.8 production deployment and repeats the live verification.
